# PostgreSQL for Railway Docker builds (fixes "Railpack" / no build plan errors).
# Set POSTGRES_USER, POSTGRES_PASSWORD, POSTGRES_DB in Railway → Variables before first deploy.
FROM postgres:16-alpine

EXPOSE 5432
